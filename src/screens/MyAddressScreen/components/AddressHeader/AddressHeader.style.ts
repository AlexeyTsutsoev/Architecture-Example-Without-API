import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginVertical: 20,
  },
  marginForFirst: {
    marginRight: 11,
  },
});

export default styles;
